#include <bits/stdc++.h>

using namespace std;


void work(){
    int n;
    cin >> n;
    vector<int> a(n),b(n);

    for (auto &c : a) cin >> c;
    for (auto &c : b) cin >> c;

    long long prev = abs(a[0]-b[0]);

    for (int i=1;i<n;i++){
        prev = min()
    }

    cout << lo << '\n';
}


int main(){

    int t;
    cin >> t;
    while (t--) work();

    return 0;
}